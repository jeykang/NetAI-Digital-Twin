# -----------------------------
# Secrets / local configuration
# -----------------------------
.env
.env.*
!.env.example
example.env
docker-compose.override.yml
!docker-compose.override.yml.example

# -----------------------------
# Python
# -----------------------------
__pycache__/
*.py[cod]
*.pyd
*.so
.venv/
venv/
.envrc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# -----------------------------
# Jupyter
# -----------------------------
.ipynb_checkpoints/

# -----------------------------
# OS / editor
# -----------------------------
.DS_Store
Thumbs.db
.vscode/
!.vscode/extensions.json
!.vscode/settings.json

# -----------------------------
# Lakehouse stack generated data
# -----------------------------
minio_data/
trino_data/

# Superset data (metadata DB uses a named volume by default, but keep these ignored
# in case someone switches back to bind mounts)
superset_home/
superset_db_data/

# Logs
*.log
logs/

# nuScenes
user_data/

kaist_ingestion/

ngc_key.txt

netai-e2e/
netai-e2e-orig/

# -----------------------------
# Demo wall display — keep the code (html/css/js/py/md + cam1-3.jpg fallback
# frames), exclude generated data & build artifacts (all regenerable).
# NOTE: gitignore has no inline comments — keep patterns on their own lines.
# -----------------------------
# extracted real-clip library (regen: extract_assets.py)
demo_wall/assets/clips/
# portable build output (regen: build_portable.py)
demo_wall_portable/
# zipped portable bundles for USB transfer
demo_wall_portable*.zip

# Auto-downloaded ML model weights (not source)
yolov8n.pt
*.pt
# Persistent Polaris (Iceberg REST catalog) metastore
polaris_pg_data/
nvidia_ingestion/_ood_clips.txt
nvidia_ingestion/_ood_clusters.csv
