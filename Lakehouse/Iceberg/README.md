> [!NOTE]
> **Prerequisite:** Create a `spark1` bucket at http://localhost:9001 before proceeding.


Command to run the Spark job inside the `spark-iceberg` container: 
```
spark-submit spark-iceberg-nessie_test.py
```